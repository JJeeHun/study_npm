module.exports = {ko:'안녕' ,en:'hi' ,fr:'salut'}

/**
 * @NPM_scripts 사용법
 * 
 * npm run [scripts property] -> npm run test
 */

/**
 * @NPM_outdated
 * 
 * 1. npm outdated -> 업데이트 가능한 패키지가 있는지 확인
 * 2. npm update -> 패키지 업데이트
 * !) package.json 에서 버전설정을 통해 업데이트를 실행 -> ~1.0.1 = 맨뒤에 버전(bug fix)만 update
 */

/**
 * @NPM_export
 * 
 * 0. npm login                        => 명령어 입력 후 로그인
 * 1. npm publish --access public      => npm에 public 하게 올림
 * 2. npm publish                      => npm에 수정 파일 업로드 package.json에 버전 변경    
 */

/**
 * @NPM_cli_명령어
 * 
 * 0. npm login                        => 명령어 입력 후 로그인
 * 1. npm publish --access public      => npm에 public 하게 올림
 * 2. npm publish                      => npm에 수정 파일 업로드 package.json에 버전 변경    
 */

